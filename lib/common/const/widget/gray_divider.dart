import '../../common_package.dart';

class GrayDivider extends StatelessWidget {
  const GrayDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 8,
      color: GRAY_10,
    );
  }
}
